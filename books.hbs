{{!< default}} <div class="container clearfix">
    <main role="main" class="content">
        <a class="btn" href="{{@blog.url}}" title="Back to homepage">&laquo; Back to home</a>
        <div class="post no-index">
            <h1>📚 Books </h1>
            <code>Short summaries and notes of books I've read, includes quotes and some relevant ideas for future reference</code>
        </div>
        <hr/>
                {{#get "posts" limit='all' filter="tags:books"}}
                {{#foreach posts}}
                        <article class="{{post_class}} post">
                            <h3><a href="{{url}}" title="{{title}}">{{title}}</a></h3>
                            <div class="excerpt">{{excerpt characters="160"}}&hellip;<a class="read-more" href="{{url}}">▷ read more</a></div>
                            </footer>
                        </article>
                {{/foreach}}
                {{/get}}
        {{pagination}}
    </main>
    </div>